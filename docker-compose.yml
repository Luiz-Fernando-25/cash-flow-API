version: "3.1"
services:
  db:
    image: mysql
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - cashflow_date:/var/lib/mysql

volumes:
  cashflow_date:
